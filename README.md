# Indhu-.S
To implement the game, we need to follow the steps listed below:  Generate a random number using the Math.random() method. Take user input using Scanner. Compare the guess with the random number and provide feedback. Limit the number of attempts using a loop. Handle edge cases (e.g., invalid inputs).
